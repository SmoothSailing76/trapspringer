# Repository Workflow

## Create Repo

Download ZIP, extract, `git init`, add `.gitignore`, `git add .`, commit, create GitHub repo, add remote, push.

## Update Repo

Download latest build ZIP, extract somewhere temporary, copy files into local repo without replacing `.git`, run `git status`, `git add .`, commit, push.
