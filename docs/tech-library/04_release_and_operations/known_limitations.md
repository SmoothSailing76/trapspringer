# Known Limitations

The build supports the DL1 main path but not every optional branch. AD&D 1e rules are main-path focused and incomplete in edge cases. Party simulation is improving but not human-equivalent. Mapping has true/public separation but no polished graphical UI. Replay supports main-path checkpoints; exhaustive branch replay needs more coverage.
